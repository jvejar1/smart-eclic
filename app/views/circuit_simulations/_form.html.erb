<%= form_with(model: circuit_simulation, local: true) do |form| %>
  <% if circuit_simulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(circuit_simulation.errors.count, "error") %> prohibited this circuit_simulation from being saved:</h2>

      <ul>
      <% circuit_simulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= label_tag :schematic_link,"(AE)Esquematico:" %>
    <%=if circuit_simulation.schematic.attached? then link_to "Descargar", rails_blob_path( circuit_simulation.schematic, disposition: "attachment") else "No adjunta" end %>
  </div>

  <!---->
  <!--<div class="field">-->
    <!--<%= form.label :user_prelab_id %>-->
    <!--<%= form.text_field :user_prelab_id %>-->
  <!--</div>-->

  <!--<div class="field">-->
    <!--<%= form.label :circuit_id %>-->
    <!--<%= form.text_field :circuit_id %>-->
  <!--</div>-->

  <div class="field">
    <%= form.label :grade,"(E) Nota:" %>
    <%= form.number_field :grade %>
  </div>
  <div class="field">
    <%= form.label :schematic ,"(A)Adjuntar"%>
    <%= form.file_field :schematic %>
  </div>

  <div class="actions">
    <%= form.submit "Subir"%>
  </div>
<% end %>
